# Outstanding

- Restart `npm run dev` to verify the app boots after dependency changes.
- Manually validate XLSX export opens correctly in Excel/Sheets.
- Review print/PDF output for layout and metadata accuracy.
